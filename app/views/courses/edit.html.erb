<% title "Edit" %>

<h3>Edit</h3>
<%= simple_form_for @course, :method => :put,
                             :class => 'form-horizontal' do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <%= f.button :submit, 'Update', :class => 'btn-primary' %>
<% end %>
